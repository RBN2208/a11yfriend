'use client'

import {useTranslations} from "next-intl";

type Props = {}

export default function ProfileSettingsAccountMenu(props: Props) {
  const t = useTranslations();

  return (
    <>
       Account Menu
    </>
  )
}
